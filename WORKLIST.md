# Worklist Queue

Purpose: keep the live media-factory queue readable. Historical duplicate publication notes and queue-overlay churn now live in `AUDIT_LOG.md`.

## Status Keys
- `queued`
- `in_progress`
- `blocked`
- `done`

## Fleet execution sequence (cross-shard)

- P0 foundation: complete `core/WL-200` first, then run `DR-120` and launch trust/recovery surface parity slices in parallel (`ui/WL-240`, `hub/WL-240`, `mobile/WL-027`, `hub-registry/WL-260`, `media/MF-014`).
- P1 hardening: after `WL-200` is stable and `design/DR-124` is in review, execute `core/WL-201`, then the follow-on clarity/visibility slices (`ui/WL-241`, `hub/WL-241`, `hub-registry/WL-261`, `hub-registry/WL-262`, `mobile/WL-028`, `media/MF-015`).
- P2 quality finish: gate on `design/DR-121` through `design/DR-127` before closure on `design/DR-123`; complete ui-kit flagship reliability slice in the same cycle after cross-shard copy/state/known-issue coherence settles.

## Queue
| ID | Status | Priority | Task | Owner | Notes |
|---|---|---|---|---|---|
| MF-011 | done | P1 | Close `C1c` by inventorying the live document, preview, route, video, and archive adapter families and proving provider choice is media-factory-private, switchable, and kill-switchable. | agent | Closed 2026-03-19: `MEDIA_ADAPTER_MATRIX.md` plus `MEDIA_CAPABILITY_SIGNOFF.md` now keep every media family under owner-repo contracts/runtime, while preview backend choice remains switchable and kill-switchable through media-factory-private controls. |
| MF-012 | done | P1 | Close `E4` by making document, portrait, and bounded-video capabilities stable, approval-aware, provenance-rich, and operator-verifiable. | agent | Closed 2026-03-19: document, portrait, bounded-video, route, and archive capability families are now tied to explicit owner contracts, runtime lifecycle governance, restore proof, and operator signoff in `MEDIA_CAPABILITY_SIGNOFF.md`. |
| MF-013 | done | P1 | Close the media-factory share of `F1` by publishing storage, retention, restore, and replay-safe operator evidence for asset/render workloads. | agent | Closed 2026-03-19: `MEDIA_FACTORY_RESTORE_RUNBOOK.md` plus `Chummer.Media.Factory.Runtime.Verify` now prove backup contract stability, restore continuity, replay counters, and retention sweep behavior for asset/render workloads. |
| MF-009 | done | P1 | Publish runtime integration evidence for service handoff from hub into media-factory write/read models. | agent | Closed 2026-03-19: `Chummer.Media.Factory.Runtime` now owns the asset/render execution code and `run-services` clean-room verification consumes that owner-repo assembly instead of local source. |
| MF-010 | done | P1 | Remove scaffold-stage assumptions from production path and prove one end-to-end media job survives a full render-to-asset flow under operator-run defaults. | agent | Closed 2026-03-19: local verify builds the runtime project, and the cross-repo smoke/verifier exercise full render-job to asset-lifecycle flow under the owner-repo boundary. |
| MF-006 | done | P1 | Materialize milestone coverage for the scaffold-stage extraction. | agent | Closed: milestone truth is explicit, and the repo no longer pretends “scaffold exists” means “boundary is complete.” |
| EXTRACT-001A | done | P1 | Make `Chummer.Media.Contracts` a real canonical render-only package plane. | agent | Closed: package metadata, namespace policy, Release pack verification, and evidence are in place. |
| EXTRACT-006 | done | P1 | Prove hub-to-media seam cutover expectations instead of hand-waving them. | agent | Closed: acceptance artifacts and verification now spell out the render-only handoff contract. |
| EXTRACT-007 | done | P1 | Materialize the asset-kernel backlog and completion evidence. | agent | Closed: manifests, binary storage, render jobs, previews, TTL/retention, and lineage expectations are explicit and evidenced. |
| EXTRACT-008 | done | P1 | Keep media DTOs render-only and push narrative/delivery meaning upstream. | agent | Closed: DTO boundary split evidence now exists instead of hiding in queue prose. |
| MF-007 | done | P2 | Archive duplicate queue/publication churn out of the live media worklist. | agent | Completed 2026-03-14: historical queue replay moved to `AUDIT_LOG.md`, and this file now reflects the real service boundary. |

| MF-014 | queued | P0 | Improve media pipeline UX by adding clear progress, cancellation, and success/error states across preview/document/portrait/video lanes. | agent | This slice adds user-facing determinism and recovery messaging so users can trust async generation outcomes without opening internal logs. Depends on `core/WL-200` and `design/DR-120` before kickoff. Evidence target: `METRICS_AND_SLOS.yaml` `artifact_publication_integrity` and media user-help copy coverage from `design/DR-124`. Acceptance checks: (1) progress/queue/cancel states are explicit and consistent for all media lanes; (2) fallback and recovery messaging is identical across desktop/mobile/help entrypoints; (3) no "stuck" state without user-safe next action. |
| MF-015 | queued | P1 | Add artifact quality and lineage visibility into media outputs so contributors can reason about generation source, policy, and retention at the UX level. | agent | This slice improves troubleshooting and support quality while preserving the existing render-only boundary split. Depends on `media/MF-014` and `core/WL-201` (plus `design/DR-121`) before execution and must satisfy `design/DR-127` copy-risk closure if any user-facing remediation text is added. Acceptance checks: (1) provenance and lineage copy is present with clear policy/retention scope; (2) output review workflow maps to the same known-issue status language as support closure guidance (`design/DR-126`); (3) `design/DR-125` throughput/reliability claims hold for async generation completion. |
| MF-016 | queued | P2 | Add consistent retry/cancel/restore messaging plus safe fallback for long-running generation queues. | agent | This slice keeps creators from losing trust when async jobs fail or stall. Depends on `media/MF-015` and `design/DR-129`; implemented in parity with run/UI support copy. Acceptance checks: (1) long-running media job states include deterministic fallback and resume status; (2) retry/cancel/restore are consistently worded and locale-safe with `design/DR-128`; (3) `METRICS_AND_SLOS.yaml` includes a no-loss recovery metric for failed generation runs. |
| MF-017 | queued | P2 | Add one-click action history and "what changed" provenance card for regenerated outputs so users can compare before/after confidently. | agent | This slice reduces user confusion around media updates and reruns and supports support triage. Depends on `media/MF-016` and `design/DR-129`; keep implementation render-only and read-model safe. Acceptance checks: (1) every regeneration action stores a provenance snapshot in user-facing output context; (2) rollback and comparison notices map to one canonical status language from `design/DR-126`; (3) no destructive override without explicit confirmation and recovery copy. |

## Current repo truth

- Repo-local live queue: none
- This repo is no longer scaffold-stage for asset/render execution: source, mirrors, milestone truth, and integration coverage are all live locally
- Remaining work is additive provider depth and capability polish, not missing adapter ownership or media-plane release proof
- Milestone-coverage modeling slice (`Finish milestone coverage modeling for media-factory so ETA and completion truth are no longer partial.`) is complete in `.codex-design/repo/IMPLEMENTATION_SCOPE.md` (`M0`..`M8` mapping with explicit gates and ETA/completion basis).
- Current queue head in `.codex-studio/published/QUEUE.generated.yaml` remains the milestone-coverage slice, followed by seam/DTO/package/asset duplicate families. Active duplicate families map to existing evidence as follows: seam -> `EXTRACT-006`, asset-kernel -> `EXTRACT-007`, DTO split -> `EXTRACT-008`, package plane -> `EXTRACT-001A`.

## Historical log

- Re-entry `2026-04-10` (milestone-coverage modeling closure revalidation, /fast system re-entry latest run): re-read the required project/design/repo/review/worklist/queue files from disk (including `AGENTS.md` and project scope at `/docker/chummercomplete/chummer-design/products/chummer/projects/media-factory.md`), confirmed feedback remains fully applied via `feedback/.applied.log` with no unread files pending, confirmed `scripts/ai/set-status.sh` is not present in this repo, revalidated `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-04-10` (milestone-coverage modeling closure revalidation, /fast system re-entry this run): re-read all required project/design/repo/review/worklist/queue inputs from disk (including `AGENTS.md`), confirmed no unread feedback files were pending relative to `feedback/.applied.log`, revalidated `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-04-10` (milestone-coverage modeling closure revalidation, /fast system re-entry current slice): read all required project/design/repo/review/worklist/queue inputs from disk (including `AGENTS.md`), processed feedback files oldest-first and marked `feedback/2026-04-09-221800-audit-task-45275.md` plus `feedback/2026-04-10-github-review-pr.md` as READ for this pass, revalidated `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-04-10` (seam runnable-backlog closure revalidation, /fast system re-entry latest pass): read required project/design/repo/review/worklist/queue files from disk in this run (including `AGENTS.md` and feedback files oldest-first), confirmed `scripts/ai/set-status.sh` is not present in this repo, reaffirmed the active seam runnable-backlog prompt remains satisfied by existing `EXTRACT-006` backlog and execution evidence (`docs/EXTRACT-006-run-services-seam-cutover-backlog.md`, `docs/EXTRACT-006-SEAM-execution-evidence.md`, gates `SEAM-01`..`SEAM-04`), and reran `bash scripts/ai/verify.sh` successfully; no additional runnable backlog units were required.
- Re-entry `2026-04-10` (seam runnable-backlog closure revalidation, /fast current slice): read required project/design/repo/review/worklist/queue files from disk in this run (including `AGENTS.md` and feedback files oldest-first), confirmed feedback application status remains complete via `feedback/.applied.log`, reaffirmed the active seam runnable-backlog prompt is still satisfied by existing `EXTRACT-006` backlog and execution evidence (`docs/EXTRACT-006-run-services-seam-cutover-backlog.md`, `docs/EXTRACT-006-SEAM-execution-evidence.md`, gates `SEAM-01`..`SEAM-04`), and reran `bash scripts/ai/verify.sh` successfully; no additional runnable backlog units were required.
- Re-entry `2026-04-10` (milestone-coverage modeling closure revalidation, /fast system re-entry latest pass): read required project/design/repo/review/worklist/queue files from disk in this run (including `AGENTS.md` plus feedback files oldest-first), confirmed feedback application status remains complete via `feedback/.applied.log`, revalidated `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-04-10` (milestone-coverage modeling closure revalidation, /fast current pass): re-read all required project/design/repo/review/worklist/queue files from disk (including `AGENTS.md` and feedback files oldest-first), reconfirmed `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still provides complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-04-10` (milestone-coverage modeling revalidation, /fast system re-entry current pass): read all required project/design/repo/review/worklist/queue files from disk (including `AGENTS.md`), confirmed feedback status remains fully applied via `feedback/.applied.log` with no unread feedback files pending, revalidated `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-04-09` (milestone-coverage re-restoration, /fast system re-entry current pass): read all required project/design/repo/review/worklist/queue files from disk in the prescribed order (including `AGENTS.md`), confirmed feedback status is fully applied via `feedback/.applied.log` with no unread feedback files pending, restored the regressed milestone coverage model in `.codex-design/repo/IMPLEMENTATION_SCOPE.md` (explicit `M0`..`M8` program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh`.
- Re-entry `2026-04-09` (design-mirror feedback incorporation + milestone-coverage closure revalidation, /fast system re-entry): read all required project/design/repo/review/worklist/queue files from disk in the prescribed order (including `AGENTS.md`) plus unread feedback oldest-first (`feedback/2026-04-09-221800-audit-task-45275.md`), synced the approved design mirror bundle from `/docker/chummercomplete/chummer-design/products/chummer` into `.codex-design/product/*` and `.codex-design/repo/IMPLEMENTATION_SCOPE.md`, confirmed `.codex-design/review/REVIEW_CONTEXT.md` remains AGENTS-aligned for render-boundary P1 checks and verification flow, and reran `bash scripts/ai/verify.sh`; milestone coverage truth remains complete for `M0`..`M8` with no additional implementation backlog delta required.
- Re-entry `2026-04-09` (milestone-coverage modeling revalidation, /fast system re-entry this pass): read all required project/design/repo/review/worklist/queue files from disk (including `AGENTS.md`), confirmed feedback remains fully applied via `feedback/.applied.log` with no unread feedback files pending, reconfirmed `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-04-09` (milestone-coverage modeling revalidation, /fast system re-entry newest pass): read all required project/design/repo/review/worklist/queue files from disk (including `AGENTS.md`), confirmed feedback remains fully applied via `feedback/.applied.log` with no unread feedback files pending, reconfirmed `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-04-09` (milestone-coverage modeling revalidation, /fast system re-entry slice pass): read all required project/design/repo/review/worklist/queue files from disk (including `AGENTS.md`), confirmed feedback remains fully applied via `feedback/.applied.log` with no unread feedback files pending, revalidated `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-04-09` (milestone-coverage modeling revalidation, /fast system re-entry current pass): read all required project/design/repo/review/worklist/queue files from disk (including `AGENTS.md`), confirmed feedback status remains fully applied via `feedback/.applied.log`, revalidated `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-04-09` (seam runnable-backlog revalidation, /fast system re-entry): read required project/design/repo/review/worklist/queue files from disk in the prescribed order (including `AGENTS.md`), confirmed no unread feedback remained via `feedback/.applied.log`, reaffirmed the current seam runnable-backlog prompt is still satisfied by existing `EXTRACT-006` backlog/evidence (`docs/EXTRACT-006-run-services-seam-cutover-backlog.md`, `docs/EXTRACT-006-SEAM-execution-evidence.md`, gates `SEAM-01`..`SEAM-04`), and reran `bash scripts/ai/verify.sh` successfully; no additional runnable backlog units were required.
- Re-entry `2026-04-09` (milestone-coverage modeling revalidation, /fast system re-entry): read all required project/design/repo/review/worklist/queue files from disk in the prescribed order (including `AGENTS.md`), checked feedback status via `feedback/.applied.log` and confirmed no unread feedback remained, revalidated `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-04-08` (milestone-coverage modeling revalidation, /fast system re-entry): read required project/design/repo/review/worklist/queue files from disk (including `AGENTS.md`), confirmed feedback status remains fully applied (`feedback/.applied.log` covers all files in `feedback/`), revalidated `.codex-design/repo/IMPLEMENTATION_SCOPE.md` and `.codex-design/review/REVIEW_CONTEXT.md` still satisfy milestone-coverage and AGENTS P1 review-contract requirements, and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-04-08` (milestone-coverage modeling revalidation, system re-entry): read required project/design/repo/review/worklist/queue files from disk (including `AGENTS.md`) and feedback status oldest-first, reconfirmed `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-04-08` (milestone-coverage modeling closure truth fix, system re-entry): re-read required design/repo/review/worklist/queue files from disk, confirmed `.codex-design/repo/IMPLEMENTATION_SCOPE.md` already has complete explicit milestone coverage modeling for `M0`..`M8`, and fixed `WORKLIST.md` queue-head drift so milestone closure is recorded as complete while seam runnable-backlog duplicate remains the active queue head; `bash scripts/ai/verify.sh` rerun in this pass.
- Re-entry `2026-04-08` (milestone-coverage queue-head revalidation, system re-entry): re-read all required product/repo/review/worklist/queue files from disk (including `AGENTS.md`) plus feedback files oldest-first, confirmed `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still carries explicit complete `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-04-08` (milestone-coverage queue-head closure + design-mirror refresh feedback incorporation): re-read all required product/repo/review/worklist/queue files from disk plus unread feedback oldest-first, incorporated `feedback/2026-04-07-181202-audit-task-45275.md`, restored explicit milestone coverage modeling in `.codex-design/repo/IMPLEMENTATION_SCOPE.md` (full `M0`..`M8` program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), refreshed `.codex-design/review/REVIEW_CONTEXT.md` with AGENTS-aligned P1 checks and required verification flow, and reran `bash scripts/ai/verify.sh`.
- Re-entry `2026-03-31` (milestone-coverage queue-head closure revalidation, current pass): re-read all required product/repo/review/worklist/queue inputs from disk (including `AGENTS.md` and feedback status via `feedback/.applied.log`), reconfirmed `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-03-31` (milestone-coverage queue-head closure revalidation, system re-entry pass): re-read all required product/repo/review/worklist/queue inputs from disk (including `AGENTS.md`), confirmed no unread feedback action was required for this run, validated `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-03-31` (milestone-coverage queue-head closure revalidation, required-read pass): re-read all required product/repo/review/worklist/queue inputs from disk (including `AGENTS.md` and feedback files in oldest-first order), revalidated `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `bash scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-03-31` (milestone-coverage queue-head closure revalidation, no-delta pass): re-read all required product/repo/review/worklist/queue inputs from disk, confirmed no unread feedback files were pending for this run, reconfirmed `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still carries complete explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, and status), and reran `scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-03-31` (milestone-coverage queue-head closure revalidation, ordered feedback replay): re-read all required product/repo/review/worklist/queue inputs from disk, reprocessed feedback files oldest-first (`feedback/2026-03-14-014908-audit-task-22426.md`, `feedback/2026-03-10-public-repo-graph-audit.md`, `feedback/2026-03-14-github-review-pr.md`, `feedback/2026-03-21-github-review-pr.md`) per status metadata, reconfirmed `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still carries explicit complete `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status), and reran `scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-03-31` (milestone-coverage queue-head closure revalidation): re-read all required product/repo/review/worklist/queue inputs from disk, confirmed no unread feedback files were required for this run, validated `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, and status), and reran `scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-03-31` (milestone-coverage closure revalidation): re-read all required product/repo/review/worklist/queue inputs from disk plus feedback status, confirmed no unread feedback remained, validated `.codex-design/repo/IMPLEMENTATION_SCOPE.md` still contains explicit `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, and status), and reran `scripts/ai/verify.sh` successfully; no additional implementation delta was required for this slice.
- Re-entry `2026-03-31` (milestone-coverage queue-head re-restoration): re-read required design/review/worklist/queue context from disk, confirmed the active queue head remains `Finish milestone coverage modeling for media-factory so ETA and completion truth are no longer partial.`, and restored the missing implementation-scope milestone coverage model in `.codex-design/repo/IMPLEMENTATION_SCOPE.md` with explicit `M0`..`M8` program mapping, contract set, coverage sources, completion gates, ETA/completion basis, and status after regression to scope-only content.
- Re-entry `2026-03-30` (milestone-coverage queue-head restoration): re-read required design/review/worklist/queue context from disk, confirmed queue-head slice is still `Finish milestone coverage modeling for media-factory so ETA and completion truth are no longer partial.`, and restored explicit `M0`..`M8` milestone coverage modeling in `.codex-design/repo/IMPLEMENTATION_SCOPE.md` (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status) after regression to scope-only text.
- Re-entry `2026-03-21` (DTO runnable-backlog slice closure validation): re-read required design mirror + repo context files, confirmed there were no unread feedback files in `feedback/`, and reaffirmed that the active queue prompt `Publish or append runnable backlog for Media DTOs still need to be split cleanly between downstream render/job/asset lifecycle contracts and upstream narrative-authoring, delivery, and campaign-context contracts..` remains satisfied by existing `EXTRACT-008` runnable backlog plus DS execution evidence (`docs/EXTRACT-008-dto-split-boundary-backlog.md`, `docs/EXTRACT-008-DS-execution-evidence.md`, gates `DS-01`..`DS-05`); contract boundary verification passed and no additional runnable backlog units were required.
- Re-entry `2026-03-21` (seam runnable-backlog slice + compatibility contract hardening): re-read required design/review/worklist/queue inputs and unread feedback oldest-first (`feedback/2026-03-14-github-review-pr.md`, then `feedback/2026-03-21-github-review-pr.md`), reaffirmed the seam runnable-backlog prompt as already satisfied by existing `EXTRACT-006` backlog/evidence, and remediated open contract regressions by aligning compatibility approval semantics to `Pending/Approved/Rejected`, removing upstream context identifiers (`SceneId`, `RouteContextId`, `RouteContext`) from compatibility render DTOs, and extending `scripts/ai/contract-boundary-tests.sh` to fail on `Scene*` and `RouteContext*` leaks; verification passed via `bash scripts/ai/contract-boundary-tests.sh` and `bash scripts/ai/verify.sh`.
- Re-entry `2026-03-21` (seam runnable-backlog slice + PR review remediation): incorporated unread feedback oldest-first including `feedback/2026-03-21-github-review-pr.md`, reaffirmed the seam runnable-backlog prompt as already satisfied by `EXTRACT-006` backlog/evidence, and fixed the reported boundary regressions by removing campaign/narrative semantics from compatibility DTO contract members (`RouteContextId`/`SceneId`, `RouteContext` target kind) plus restoring compatibility-folder enforcement in `scripts/ai/contract-boundary-tests.sh`; verification passed via `bash scripts/ai/contract-boundary-tests.sh` and `bash scripts/ai/verify.sh`.
- Full queue-overlay churn, duplicated publication prompts, and audit replay notes now live in `AUDIT_LOG.md`.
- Re-entry `2026-03-21` (milestone-coverage queue-head closure validation, ordered feedback incorporation): reprocessed the currently specified unread feedback in listed order (`feedback/2026-03-14-014908-audit-task-22426.md`, then `feedback/2026-03-10-public-repo-graph-audit.md`) and reconfirmed that `.codex-design/repo/IMPLEMENTATION_SCOPE.md` already provides complete `M0`..`M8` milestone coverage modeling (program mapping, contract set, coverage sources, completion gates, ETA/completion basis, status) with no additional executable backlog units required for this slice.
- Re-entry `2026-03-21` (milestone-coverage queue-head closure validation): reprocessed unread feedback oldest-first (`feedback/2026-03-10-public-repo-graph-audit.md`, then `feedback/2026-03-14-github-review-pr.md`) and revalidated that milestone coverage modeling is already complete in `.codex-design/repo/IMPLEMENTATION_SCOPE.md` (explicit `M0`..`M8` program mapping, contract set, coverage sources, completion gates, ETA/completion basis, and complete status for each row); no additional executable backlog units were required for the current queue-head slice.
- Re-entry `2026-03-21` (milestone coverage registry + review-contract regression repair): reprocessed unread feedback oldest-first (`feedback/2026-03-10-public-repo-graph-audit.md`, then `feedback/2026-03-14-014908-audit-task-22426.md`, then `feedback/2026-03-14-github-review-pr.md`) and reaffirmed source-first + mirror coverage posture while restoring explicit milestone coverage modeling in `.codex-design/repo/IMPLEMENTATION_SCOPE.md` (per-milestone program mapping, coverage sources, completion gates, ETA/completion basis for `M0`..`M8`) plus restored review-contract checklist coverage in `.codex-design/review/REVIEW_CONTEXT.md` (required verification steps and AGENTS-aligned P1 checks including engine/play/ui-kit dependency leakage and approval/persist/reject lifecycle coverage).
- Re-entry `2026-03-14` (seam milestone-mapping slice): the active queue prompt `Add milestone mapping or executable queue work for Run-services still owns the effective media seam, so this repo is scaffold-stage rather than a live render-only boundary..` was reaffirmed against existing `EXTRACT-006` mapping and execution evidence (`SEAM-01`..`SEAM-04`, `docs/EXTRACT-006-run-services-seam-cutover-backlog.md`, `docs/EXTRACT-006-SEAM-execution-evidence.md`); no new executable backlog units were required.
- Re-entry `2026-03-14`: package-plane milestone-mapping slice was reaffirmed against `EXTRACT-001A` (`CP-01`..`CP-03`) with no new executable backlog delta.
- Re-entry `2026-03-14` (package-plane runnable backlog slice): the active queue prompt `Publish or append runnable backlog for The repo exists, but \`Chummer.Media.Contracts\` is not yet real as the canonical render-only package plane..` was reaffirmed against `docs/EXTRACT-001A-canonical-package-plane-runtime-backlog.md` and `docs/EXTRACT-001A-canonical-package-plane-evidence.md`; no new runnable backlog units were required.
- Re-entry `2026-03-14` (DTO runnable backlog slice): unread feedback was incorporated oldest-first (`feedback/2026-03-14-github-review-pr.md`), and the active DTO runnable prompt remains satisfied by existing `EXTRACT-008` runnable backlog plus DS execution evidence (`DS-01`..`DS-05`); no new runnable backlog units were appended.
- Re-entry `2026-03-14` (asset-kernel runnable backlog slice): the active queue prompt `Publish or append runnable backlog for The asset kernel still needs manifests, binary storage, render jobs, previews, TTL, retention, and lineage before renderer-specific pipelines move here..` was reaffirmed against existing `docs/EXTRACT-007-asset-kernel-implementation-backlog.md` and `docs/EXTRACT-007-AK-execution-evidence.md`; this slice is considered satisfied without adding new executable units.
- Re-entry `2026-03-14` (DTO milestone-mapping slice): the active queue prompt `Add milestone mapping or executable queue work for Media DTOs still need to be split cleanly between downstream render/job/asset lifecycle contracts and upstream narrative-authoring, delivery, and campaign-context contracts..` was reaffirmed against existing `EXTRACT-008` mapping/execution evidence (`DS-01`..`DS-05`, `docs/EXTRACT-008-DS-execution-evidence.md`); no new executable backlog units were required.
- Re-entry `2026-03-14` (queue truth drift fix): queue-family evidence mapping was corrected to reflect all active duplicate families in `.codex-studio/published/QUEUE.generated.yaml` (`EXTRACT-006`, `EXTRACT-007`, `EXTRACT-008`, `EXTRACT-001A`) so milestone/worklist truth no longer over-attributes coverage.
- Re-entry `2026-03-14` (review-context policy fix): incorporated `feedback/2026-03-14-github-review-pr.md` and restored the explicit P1 check in `.codex-design/review/REVIEW_CONTEXT.md` that flags dependencies on engine implementation, play implementation, or UI-kit implementation, as required by `AGENTS.md`.
- Re-entry `2026-03-14` (lifecycle policy fix): incorporated `feedback/2026-03-14-github-review-pr.md` and added the explicit P1 rule in `.codex-design/review/REVIEW_CONTEXT.md` that flags asset lifecycle state machines missing approval/persist/reject coverage, matching `AGENTS.md`.
- Re-entry `2026-03-14` (milestone registry completion slice): incorporated `feedback/2026-03-14-014908-audit-task-22426.md` and completed milestone coverage modeling in `.codex-design/repo/IMPLEMENTATION_SCOPE.md` with explicit per-milestone program mapping, coverage sources, completion gates, and ETA basis so ETA/completion truth is no longer partial.
- Re-entry `2026-03-14` (queue metadata correction for milestone slice): incorporated `feedback/2026-03-14-github-review-pr.md` and corrected `WORKLIST.md` queue truth to match `.codex-studio/published/QUEUE.generated.yaml` (10 active items, milestone-coverage slice at queue head).
- Re-entry `2026-03-14` (seam runnable backlog slice): unread feedback was incorporated oldest-first (`feedback/2026-03-14-014908-audit-task-22426.md`, then `feedback/2026-03-14-github-review-pr.md`), and the active queue prompt `Publish or append runnable backlog for Run-services still owns the effective media seam, so this repo is scaffold-stage rather than a live render-only boundary..` remains satisfied by `EXTRACT-006` runnable backlog plus seam execution evidence (`SEAM-01A`, `SEAM-01B`, `SEAM-03A`, `SEAM-03B`, `docs/EXTRACT-006-run-services-seam-cutover-backlog.md`, `docs/EXTRACT-006-SEAM-execution-evidence.md`); no new runnable backlog units were required.
- Re-entry `2026-03-14` (DTO runnable backlog slice, current queue prompt): unread feedback was incorporated oldest-first (`feedback/2026-03-14-014908-audit-task-22426.md`, then `feedback/2026-03-14-github-review-pr.md`), and the active queue prompt `Publish or append runnable backlog for Media DTOs still need to be split cleanly between downstream render/job/asset lifecycle contracts and upstream narrative-authoring, delivery, and campaign-context contracts..` remains satisfied by existing `EXTRACT-008` runnable backlog and DS execution evidence (`DS-01`..`DS-05`, `docs/EXTRACT-008-dto-split-boundary-backlog.md`, `docs/EXTRACT-008-DS-execution-evidence.md`); no new runnable backlog units were required.
- Re-entry `2026-03-14` (DTO runnable backlog slice, queue-overlay mapping clarification): updated `EXTRACT-008` DS-05 wording so prepend-mode generic queue duplicates are explicitly treated as mapped to DS-01..DS-05 runnable units instead of incorrectly requiring queue-item absence; local verification rerun passed via `bash scripts/ai/verify.sh`.
- Re-entry `2026-03-14` (feedback incorporation and milestone-map drift fix): unread feedback was processed oldest-first (`feedback/2026-03-14-014908-audit-task-22426.md`, then `feedback/2026-03-14-github-review-pr.md`), and `.codex-design/repo/IMPLEMENTATION_SCOPE.md` M8 mapping was corrected from `E3` to canonical `E4` while keeping the DTO runnable-backlog slice mapped to `EXTRACT-008` (`DS-01`..`DS-05`) with no new runnable units needed.
- Re-entry `2026-03-14` (DTO milestone-mapping slice, current): reaffirmed the active queue prompt `Add milestone mapping or executable queue work for Media DTOs still need to be split cleanly between downstream render/job/asset lifecycle contracts and upstream narrative-authoring, delivery, and campaign-context contracts..` against explicit milestone mapping now recorded in `docs/EXTRACT-008-DS-execution-evidence.md` (`M8 -> E4 -> media_execution_vnext`, gates `DS-01`..`DS-05`); no additional executable backlog units were required.
- Re-entry `2026-03-14` (review-context verification checklist restoration): incorporated `feedback/2026-03-14-github-review-pr.md` by restoring explicit test/verification prompts in `.codex-design/review/REVIEW_CONTEXT.md` (contract-boundary checks, `scripts/ai/verify.sh`, package-boundary verification, and review summary format) so review-contract enforcement is no longer partial.
- Re-entry `2026-03-14` (seam milestone-mapping slice, current queue prompt): reaffirmed the active queue prompt `Add milestone mapping or executable queue work for Run-services still owns the effective media seam, so this repo is scaffold-stage rather than a live render-only boundary..` against existing milestone coverage and seam gates in `.codex-design/repo/IMPLEMENTATION_SCOPE.md` (`M6 -> C1`, coverage sources `docs/EXTRACT-006-SEAM-execution-evidence.md` + `docs/EXTRACT-006-run-services-seam-cutover-backlog.md`, gate `SEAM-01`..`SEAM-04` + effective seam ownership handoff); no new executable backlog units were required.
- Re-entry `2026-03-14` (asset-kernel runnable backlog slice, current): unread feedback was rechecked oldest-first (`feedback/2026-03-14-014908-audit-task-22426.md`, then `feedback/2026-03-14-github-review-pr.md`), and the active queue prompt `Publish or append runnable backlog for The asset kernel still needs manifests, binary storage, render jobs, previews, TTL, retention, and lineage before renderer-specific pipelines move here..` remains satisfied by existing `EXTRACT-007` runnable backlog and AK execution evidence (`AK-01`..`AK-06`, `docs/EXTRACT-007-asset-kernel-implementation-backlog.md`, `docs/EXTRACT-007-AK-execution-evidence.md`); no new runnable backlog units were required.
- Re-entry `2026-03-14` (asset-kernel milestone-mapping slice, current queue prompt): reaffirmed the active queue prompt `Add milestone mapping or executable queue work for The asset kernel still needs manifests, binary storage, render jobs, previews, TTL, retention, and lineage before renderer-specific pipelines move here..` against explicit `EXTRACT-007` milestone mapping now recorded in `docs/EXTRACT-007-AK-execution-evidence.md` (`M1 -> C1 -> media_execution_vnext`, gate `AK-01`..`AK-06`); no new executable backlog units were required.
- Re-entry `2026-03-14` (design-bundle sync + review-context refresh slice, current queue prompt): unread feedback was rechecked oldest-first (`feedback/2026-03-14-014908-audit-task-22426.md`, then `feedback/2026-03-14-github-review-pr.md`), `.codex-design/` mirror files were validated as present and aligned with current local approved design context, and `.codex-design/review/REVIEW_CONTEXT.md` already includes explicit render-boundary P1 checks plus required verification prompts (`scripts/ai/contract-boundary-tests.sh`, `scripts/ai/verify.sh`, package-boundary/lifecycle checks, review summary format); no additional mirror content delta or runnable backlog units were required for this queue item.
- Re-entry `2026-03-14` (package-plane milestone-mapping slice, current queue prompt): unread feedback was rechecked oldest-first (`feedback/2026-03-14-014908-audit-task-22426.md`, then `feedback/2026-03-14-github-review-pr.md`), and the active queue prompt `Add milestone mapping or executable queue work for The repo exists, but \`Chummer.Media.Contracts\` is not yet real as the canonical render-only package plane..` is now explicitly mapped in `docs/EXTRACT-001A-canonical-package-plane-evidence.md` (`M0 -> C1 -> media_execution_vnext`, gate `CP-01`..`CP-03`); no additional executable backlog units were required.
